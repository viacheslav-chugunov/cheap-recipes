<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dev.recipes">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.application.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Recipes">
        <activity android:name=".app.presentation.detail.DetailActivity"
            android:parentActivityName=".app.presentation.category.CategoryActivity" />
        <activity android:name=".app.presentation.dish.DishActivity"
            android:parentActivityName=".app.presentation.category.CategoryActivity" />
        <activity android:name=".app.presentation.category.CategoryActivity" />
        <activity android:name=".app.presentation.split.SplitActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>